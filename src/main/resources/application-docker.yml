spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/medibridge_lab?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
  kafka:
    bootstrap-servers: kafka-container:9092
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
